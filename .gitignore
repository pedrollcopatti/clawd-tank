.superpowers/
.worktrees/

# ESP-IDF
bsp/esp-idf/
.espressif/
host/.envrc

# Firmware build artifacts

# Python
host/venv/
host/.venv/
host/.direnv/
*.pyc
__pycache__/
.pytest_cache/

# py2app build artifacts
host/build/
host/dist/
host/.eggs/

# Simulator build artifacts

# Firmware test binaries

# Screenshots
shots/

# Debug / scratch
.debug/

# Asset sources (binary, not code)
*.piskel
clawd0.png

# IDE / tooling caches
.DS_Store

# Misc
firebase-debug.log
host/clawd_tank_menubar/_version_info.py
.gitnexus

# Claude Code local state — ignore worktrees/settings/tool-generated skills,
# but track authored project skills (e.g. .claude/skills/releasing).
.claude/*
!.claude/skills/
.claude/skills/gitnexus/

# One-off / personal docs
gifs/
docs/master/
docs/posts/
docs/claude_hooks_integration_plan.md
docs/hooks_reference.md
assets/svg-animations/prompts/
local/

# Leftover local build output from the removed firmware/simulator.
# The sources are gone from the repo (see git history); these directories may
# still exist in old working copies full of ESP-IDF and CMake output.
firmware/
simulator/
